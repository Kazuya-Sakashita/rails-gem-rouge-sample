<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @post.content %>
</p>

<p>
  <strong>Auther:</strong>
  <%= @post.auther %>
</p>

<%= Redcarpet::Markdown.new(BlogRender, fenced_code_blocks: true).render(@post.content).html_safe %>
<%= markdown_to_html(@post.content) %>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
